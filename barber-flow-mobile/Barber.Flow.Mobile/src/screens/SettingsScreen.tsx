import { ScreenLayout } from "../components/ScreenLayout";
import { ScreenTitle } from "../components/ui/ScreenTitle";

export const SettingsScreen = () => {
    return (
        <ScreenLayout title="Settings Screen Menu" backgroundColor="#6e6e6e" center>
            <ScreenTitle>Settings Screen</ScreenTitle>
        </ScreenLayout>);
};